---
description: 用户说「冗余梳理」时的输出格式（本仓库自包含，不依赖其他项目）
alwaysApply: true
---

# 冗余梳理

格式以 **本文件为准**，不读取、不引用其他仓库或桌宠项目文档。

用户说 **「冗余梳理」**（或同类：列冗余、审核冗余）时：

1. **只列清单，先别改代码**（除非明确说删/砍，或回编号拍板）
2. **不要用宽表**（列一多就难读）；用 **编号列表**，每条一块短文
3. 每条固定结构（可换行，别挤成一行）：

```markdown
1. **模块 · 符号/位置**
   - 现在：…
   - 应该：…（可删 / 可并 / 可内联 / 弱冗余 / 暂留 / 已完成）
   - 净效果：减码 / 持平 / **可能更胀**（必填；抽新类默认标「可能更胀」除非证明净减）
   - 留着危害：…
   - 删的危害：…
```

4. 写清归属模块；路径可跟在符号后
5. 危害两句都要写后果，别只写「低/中」
6. 勿贴大段代码
7. **末尾写推荐**：先砍哪些、停加哪些、暂留哪些；请用户回编号拍板

## 列清单前先问（防越改越胀）

**默认不是「抽 helper / 新 static / 新 Facade」**，而是按优先级选手段：

1. **删** — 死路径、双 UX 回退、一次性脚本、重复 Refresh
2. **并** — 两文件同一职责、双 Update 合成一处
3. **内联** — 只出现 1～2 次、且不会再长的 5～15 行
4. **抽共用** — 仅当 **≥3 处**、且还会继续变长；写清「少几行 / 少几个拷贝点」
5. **停加** — 已有收口层时，**本轮不再建议第 N+1 个同类类型**

## 禁止默认推荐的写法

- 「抽 `XxxHelper` / `XxxService`」——**除非**条目里算过净效果为减码
- 「收口到 `HideAllYyy()`」——若只是 3 行变 1 行调用 + 1 个新方法，标 **持平或弱收益**
- 把 **产品分叉** 标成「可抽共用」——除非用户明确要合并

## 推荐段必须包含

- **先砍**：净减码 / 删逻辑 / 并文件
- **停加**：已有 helper 层不再扩张
- **暂留**：平行系统、域外、规模未到
- 若上一轮刚拆过 `MainUISetup` 等，**本轮优先并/删，不再加新类型**

## 输出方式

**只在对话里列清单**，不写 `docs/` 下的 md 文件。
