---
description: 主界面 UI 以预制体/场景手配为准；Agent 默认只改脚本，不直接改 prefab/scene YAML
alwaysApply: true
---

# UI 手配预制体约定

与 [UI-主界面.md](../../docs/知识库/模块/UI-主界面.md) 一致：**布局与引用以场景/预制体为准**。

## 分工

| 谁 | 做什么 |
|----|--------|
| **你** | 改 `Assets/GameResources/Prefab/*.prefab`、`Demo.unity`：层级、RectTransform、拖 `[SerializeField]`、摆按钮/slot |
| **Agent** | 只改 `Assets/Scripts/**`、配表 `StreamingAssets`；**默认不编辑** `*.prefab` / `*.unity` |

除非你明确说 **「帮我改 prefab」** / **「直接写进预制体」**，否则 Agent 不碰预制体 YAML。

## Agent 加 UI 功能时

1. 脚本加字段；**运行时不要**按节点名扫 / `AddComponent` / `new GameObject` 补引用（手配或编辑器绑定为准）。
2. 在对话里写清 **挂哪、拖什么**；或让你用菜单 **UniverIdle → 一键绑定主界面引用** 补 SerializeField。
3. **不要**再建 `MainUISetup`、一键**布局坐标**生成器；允许维护已有的「一键绑定引用」Editor（`Assets/Editor/UI/MainUIBindMenu.cs`）。
4. 需要重复用的格子 → 单独 **slot 预制体** + 运行时 `Instantiate`（如 `掉落slot.prefab`），容器本身仍手配。

## 常用预制体

| 预制体 | 用途 |
|--------|------|
| `UniverIdle_MainUI.prefab` | 主界面根；`MainUIController`、`掉落预览` 等 |
| `进度条.prefab` | 挂机 RunningBar（Label / BarBg / BarFill / Time）；Fill **必须**绑 `ui_progress_fill` |
| `掉落slot.prefab` | 掉落预览单格；`LootDropSlotView`（含 Icon / Unknown） |
| `背包slot.prefab` | 背包单格；`InventorySlotView` |

## 掉落预览（当前）

- `Detail/掉落预览` → `LootPreviewView`：`slotPrefab` 拖 `掉落slot.prefab`
- `MainUIController` → `lootPreview` 拖上面的 `LootPreviewView`
- 槽位数量、? / 揭示：代码按动作 `loot` 表生成，**不用**在预览下手动摆 slot

## 背包格（当前）

- `InventoryGridView.slotPrefab` 拖 `背包slot.prefab`
- 每页按表 `SlotsPerPage` `Instantiate`，**不用**在 Body 下手动摆满格

## 一键绑定（Editor）

- 菜单：`UniverIdle/一键绑定主界面引用`；进度条另有 `UniverIdle/安装进度条预制体`（生成 `进度条.prefab` 并写回 Fill/Track 图）
- 默认目标：选中的 MainUI / App，否则 `Assets/Resources/Prefab/UniverIdle_MainUI.prefab`
- 会按节点名补引用；缺「获得提示区」或卡上 `MasteryIcon`/`MasteryLevel` 时在**编辑器**创建并绑好
- 跑完后你在 Unity 里 **保存预制体/场景**

## 禁止

- 为「少拖一次引用」去改 prefab 里大量坐标/LayoutGroup（除非你要求）
- 用 Editor 脚本偷偷改场景布局替代你的手配（绑定引用除外）
- **进度条 Fill 不绑 sprite / 只靠运行时 `Texture2D.whiteTexture`**（Filled 无图不滚；必须用 `ui_progress_fill`）
