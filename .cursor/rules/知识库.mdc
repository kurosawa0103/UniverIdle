---
description: 做功能先读知识库；模块不写变更记录，改写每日开发日志
alwaysApply: true
---

# 知识库与开发日志

## 做功能前（必读）

动手改玩法 / UI / 配表 / 存档前，先读相关知识库，**以知识库为已落地真相**：

1. [docs/知识库/模块索引.md](../../docs/知识库/模块索引.md) — 找模块
2. 对应 `docs/知识库/模块/*.md` — 职责、入口、文件、扩展
3. 需要时再看 [docs/设计/](../../docs/设计/)（只写意图，细节以知识库为准）

未读相关模块文档就开改 = 违反本约定。

## 做功能后（必写）

1. **更新知识库模块正文**（现状怎么做、挂点、限制）+ 必要时改模块索引一行
2. **不要**在模块文档里写「变更记录」表或流水账
3. **追加开发日志**：只维护一份 [docs/知识库/开发日志.md](../../docs/知识库/开发日志.md)

在该文件**顶部日期区之下**按日追加（新日期插在最上面一节）；同日合并进已有 `## YYYY-MM-DD`：

```markdown
## YYYY-MM-DD

- **模块**：做了什么（结果，不是过程）
- **模块**：…
```

## 禁止

- 在 `知识库/模块/*.md` 新增或恢复「变更记录」
- 按天拆成多个日志文件（不要 `开发日志/YYYY-MM-DD.md`）
- 用长流水代替更新正文（正文过时、只记流水 = 不合格）
- 为写日志再抽一层文档框架 / 模板生成器
