# 开发日志

按日期倒序追加；**同一天写在同一节**，条目写结果不写过程。

---

## 2026-09-04

- **冗余**：`sceneXp*`→`actionXp*`；`LeveledUp`→`ActionMastery*`；`OnWorkOrMasteryChanged`；`SceneProgressRules`→`WorkActionRules`
- **配表/进度**：`scene_levels`→`action_levels`；熟练度按 **actionId** 独立；`work_levels` 为工作总等级；存档改 `actionMasteries`（v2）
- **配表/UI**：升级经验改陡曲线（`base+per×L+0.9×L²`，1–160 重填）；获得提示加经验行；新增 `ui_xp` + `GetXp()`
- **配表**：四工作簿新增 `work_levels` / `scene_levels`（1–160）；JSON `workLevels`/`sceneLevels`；有表优先查表，末级封顶
- **配表**：works 去掉 `iconColor` / 图标颜色（拾荒·砍树·挖矿·魔物探索 Excel/JSON + 导表读写）
- **拾荒美术**：旧道驿站卡更扁平；建筑完好（不破）、无马车，屋顶加小旗
- **拾荒美术**：旧道横幅保留开阔分层构图，画风再对齐村口/黑松林纯色块扁平
- **拾荒美术**：旧道横幅保持扁平画风，构图留天空与纵深层（避免视觉压成一条）；色调仍尘土黄 + 暮紫灰天
- **拾荒美术**：冒险者旧道横幅 + 3 张 action 改 **尘土黄旅途**扁平风，与村口暖晴 / 黑松林冷青绿拉开
- **进度条**：Standard / ActionList 均手配 `runningBarRoot` + fill/文案；fill 无 sprite 时运行时补白图（否则 Filled 看不见滚动）
- **冗余**：ActionList 进度条改手配 `runningBarRoot`（删 `FindRunningBarRoot`）；Center Wire 用 `ActionCardView.ClickButton`；砍 `sceneTagsRoot` 按名找；砍未用的 `WorkDefinition.IconColor` / `GameColorUtility`
- **冗余**：`StandardWorkCenterView` 进度条改手配 `runningBarRoot`（砍 Find/扫 TMP/补白 Sprite）；`ActionCardView` 手配 `button`；背包去掉 `overlayRoot` 自身兜底
- **获得提示**：一键绑定挪父级时不再覆盖 `RectTransform` 锚点/偏移（仅新建用默认占位）
- **冗余**：砍 MainUI 双份 toast 预制体与 `BindPrefabs`、完成时再 Wire、运行时挪父级；清单去掉 `banner_gate_wide`
- **获得提示**：全局挂 `App`；错挂时一键绑定挪出（运行时不再挪），切砍树等仍显示
- **获得提示**：叠行 punch 前确保行激活；父级 inactive 时跳过协程（修 Toast inactive 报错）
- **冗余**：砍 `ActionCardView` / `LootToast*` 运行时按名 `Find`；`MainUI`/`ScavengeDetail` 不再 `GetComponentInChildren` 兜底 toast / 开工按钮文案
- **美术清理**：删除未引用的 `banner_black_pine_wide` / `banner_old_road_wide`
- **拾荒美术**：黑松林 6 张 action 按新 banner 明亮冷青绿扁平风重出（trail/fallen/shrine/hunter/stream/bandit）
- **拾荒美术**：`banner_black_pine` 用明亮冷色拉开村口（青绿针叶、粉蓝天、冷灰褐路），不压暗；仍扁平描边
- **拾荒美术**：`banner_old_road` 重出为更扁平矢量风，对齐村口、黑松林横条

## 2026-09-03

- **拾荒美术**：补黑松林 / 冒险者旧道 action 缩略图 9 张 + banner 横条 4 张（扁平 vignette 风，对齐村口）；入 `ActionImage` / `BannerImage` 与概念图 assets
- **拾荒配表**：仅保留 3 地区 14 子地点（村口 1 / 黑松林 10 / 冒险者旧道 20）；删街道·谷仓·溪滩·铁砧巷·坠星碑等旧 action
- **熟练度图标**：五角星铜/银/金三档（避金币圆牌）；按地区熟练度换档；`ItemIconLoader.GetMastery`
- **冗余 + Editor**：砍 toast/Mastery 运行时造、`GoldIconLoader` 并入 `ItemIconLoader`、MainUI/背包页签/Hub·ActionList 详情兜底；菜单 `UniverIdle/一键绑定主界面引用` 按约定补引用并可在编辑器创建获得提示区与 Mastery
- **冗余**：Format 耗时/产量/倒计入 `SceneProgressRules`；RunningBar 查找上收；砍挖矿误找拾荒详情、`AddComponent<GameSession>`；掉落格去运行时造子节点；背包格改为 `背包slot.prefab`
- **冗余**：砍顶栏 `FindNamed`/`AddComponent`/盖图/按名找引用；金币改面板自订事件只改文案；完成回调不再叠刷背包；存档脏标记重载合并
- **顶栏**：`TopBar/Currency` 手配金币 `Icon` + `Text`（`TopBarGoldView`，跟 `OnGoldChanged`）
- **存档**：默认每 10 秒自动存（脏标记）；Pause / 退出仍立刻写盘
- **拾荒**：详情独立为 `ScavengeDetailView`（仅拾荒）；Hub 改名 `ScavengeHubView`；砍树详情改绑 `WorkActionDetailView`，不再共用拾荒脚本
- **文档**：去掉模块「变更记录」；开发日志收成单一 `开发日志.md`；拾荒文案统一为地图节点/场景解锁（不再写 Tag 解锁）；索引注明 `save.dat`（JSON 文本）
- **存档**：`save.dat` 本地存档 + UniverIdle/GM 仅重置；开局陷阱；无离线结算
- **背包**：分页页签手配；容量 `inventory.json`；砍运行时造 Tabs / `AddItem` / 旧存档导出窗
- **动作卡**：未达 `requiredWorkLevel` 黑剪影 +「{工作}等级达到N解锁」+ 不可点
- **拾荒 Hub**：只激活/刷新当前地图；详情去掉按名扫引用；停机回调内联
- **约定**：rule「知识库」— 做功能先读知识库；模块不写变更记录；本文件为唯一开发日志
